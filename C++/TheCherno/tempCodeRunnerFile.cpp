is the value of a :");
    // LOG(ref);

    // //Below is pointer
    // int b = 7;
    // int *ptr = &b;
    // LOG("\nThis is the memory address of b:")
    // LOG(ptr);
    // LOG("\nThis is the value of b:")
    // LOG(*ptr);