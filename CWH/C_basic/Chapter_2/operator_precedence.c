#include<stdio.h>
#include<math.h>

int main (){
    printf("%d",4*4/8*4+5;
    // Priority : (* / %)(+ -)(=) 
    // We can control flow of program 
    // i.e. operator  can be controlled by using parenthesis :
    printf("%d",4*4/8*(4+5));
    return 0;
}


/* 

1. --> 1,3 because 3^3 will not produce 27 but it is not invalid, 
       the program will compile.
2. --> double
3. --> 
#include<stdio.h>
int main (){
    int a;
    scanf("%d",&a);
    if(a%97==0){
        printf("a is divisible by 97");
    }else{
        printf("a is not divisible by 97");
    }
    return 0;
}

*/

//frnds : s6e14